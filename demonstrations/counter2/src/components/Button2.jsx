import React from 'react'

export const Button2 = (props) => {
    // const {text} = props
    console.log(props)
  return (
    <button>B2{props.text}</button>
  )
}

export const Button3 = (props) => {
  // const {text} = props
  console.log(props)
return (
  <button>B3{props.text}</button>
)
}

